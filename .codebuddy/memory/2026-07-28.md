# 2026-07-28

- 在 `src/lib/email-scraper.ts` 的 `validateEmail` 中新增 `EMAIL_PREFIX_BLOCKLIST` 前缀黑名单（@ 前本地部分命中则 return false）。用户逐步追加了 suffix、private、privacy、example、legal，以及原有的 noreply/no-reply/donotreply/do-not-reply/postmaster/abuse/webmaster。
- 用户确认偏好：助手可对其不认可的提议提出异议（已写入 MEMORY.md）。
- 提醒用户：`legal@` 等前缀可能误伤真实业务邮箱，属用户主动决策。
