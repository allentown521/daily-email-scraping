# 2026-08-27

## launchigniter 列表入口改为首页 "/"
用户把 `launchigniter.content` 的 `matches` 改成了 `["https://launchigniter.com/"]`（精确匹配首页，之前是 `/weekly-launches/*`）。
- 同步修改 `isListPage()`：`window.location.pathname === "/" || startsWith("/weekly-launches/")`，使首页也能启动流程（SPA 导航到 `/launch/*` 详情后，`isListPage`/`isDetailPage` 判断仍然正确）。
- 详情判断 `/launch/*` 不变。
- 保持 `matches` 只精确匹配首页而非 `/*`：避免在详情页整页加载时列表脚本也注入（那样会进入 `phase="init"` 空转循环，与详情脚本冲突）。
- lint 0 报错。
