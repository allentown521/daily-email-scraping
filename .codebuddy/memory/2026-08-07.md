# 2026-08-07

## 新增 nicklaunches 选项（放在 thisWeek 分组）
- 在 `src/components/common/main.tsx` 的 **thisWeek** 分组中新增 `nicklaunches` 选项（先误建独立 weekly 分组，用户纠正后移入 thisWeek，已撤销 weekly 列，grid 保持 grid-cols-3）。
- URL 格式：`https://nicklaunches.com/week/{year}/{isoWeek}/`，复用 peerlist 的 ISO 8601 周数计算逻辑（getISOWeek）。
- 改动点：selectedSites 状态、thisWeek 分组选中逻辑、siteOptions、thisWeek 列 UI 列表。

## nicklaunches content script 去重
- `src/app/nicklaunches.content/index.tsx`：用 `Set<string>` 收集 `/products/` 链接（`urlsSet.add`）实现去重，再 `const urls = [...urlsSet]` 转数组，后续流程不变。
