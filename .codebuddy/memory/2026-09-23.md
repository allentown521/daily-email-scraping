# 2026-09-23

## 抓取时提取产品名称（productName）

- `src/lib/email-scraper.ts`：新增 `extractProductName(html, url)`，优先级 `og:site_name` → `<title>` → 域名主标签（`warmbly.com` → `Warmbly`）。
  - meta 解析不假设属性顺序（content 可写在 property 前面），只扫 `</head>` 之前的片段；title 会先解 HTML 实体（`&amp;` → `&`）。
  - title 提取品牌：按 `| • · – — - : ::` 切段（连字符需两侧有空格，避免切开 `Cold-Email`），优先取与域名主标签一致的片段，否则取最短片段；>60 字符则退回域名。
  - 背景页是 service worker 无 DOM，全部用正则实现。
- `ScrapedEmail` 新增必填字段 `name`（初版叫 `productName`，当天按用户要求改名），`scrapeEmails` 在 Level 1 拿到首页 html 后提取一次，整站（含 contact/team 等候选页邮箱）共用。提取函数仍叫 `export function extractProductName(html, url)`（未改）。
- CSV 导出（`src/hooks/useCollectedEmails.ts`）列改为 `email,name,url,time`，旧数据用 `email.name || ""` 留空。
- **第二轮修正（用户拿真实站点质问：maildoso.ai/resources/privacy-policy、saladict.cn）**：初版启发式只测了构造用例，真实站点上翻车两处，已修：
  - 子页面 title 只是页面名（"Privacy Policy"）→ 增加「片段与域名主标签相关度打分」（相等 100 / 包含 70 / 最长公共子串 ≥3 → 30+L），毫无关系的片段一律不要；且 URL 非根路径时不信任 title，直接退到域名主标签（maildoso.ai → "Maildoso"）。
  - 通用描述段（Home / 首页 / 个人主页 / Blog / Pricing …，中英文黑名单 `GENERIC_TITLE_SEGMENTS`）会被剔除，仅在「首页 + 无任何域名线索」这条兜底路径上生效（saladict.cn 的 "AllenTown | 个人主页" → "AllenTown"）。
  - 结论：10 个构造用例 + 3 个真实 URL 全部符合预期。**教训：改完必须拿真实站点（尤其是子页面、非英文站、无 og 标签的站）验证，不能只跑构造 HTML。**
- **第三轮（用户拍板「域名说什么就是什么」）**：`pickBrandFromTitle` 简化——只在 title 片段与域名主标签「有关联」时才用 title，否则一律退回域名主标签。因此删掉了第二轮加的通用词黑名单 `GENERIC_TITLE_SEGMENTS` / `isGenericTitleSegment` / `isRootPage`（已无用）。结果：saladict.cn 由 `AllenTown` 改为 `Saladict`；maildoso 子页面仍是 `Maildoso`。副作用：品牌名与域名毫无字面关联时（如 InboxHero 建在 abc-tool.com）会取域名，属于可接受的取舍。
- **真实批量验证（9 个 PH 外链，含 App Store / 403 / 无法连通三类）**：正确 6 个（arcjet→Arcjet、selltostate→Sell to State、tryflicka→Flicka、cronhq.xyz→Cronhq、milliseconds.ai→milliseconds.ai、postsider→PostSider），hyrax.dev 被 403 但靠域名兜底仍拿到 `Hyrax`（验证兜底有效）。两个已知缺陷：
  - `apps.apple.com/us/app/...`：`og:site_name="App Store"` 抢先，得到 "App Store" 而不是 App 名 "Superset"；且苹果对后台 fetch 返回的是通用页（title 为「iPhone 版"Today" - App Store」），拿不到 App 名，只能解析 URL slug（`.../app/{slug}/id...`）或特殊处理 marketplace 的 og:site_name。
  - `flow.google.com/?...`：本沙箱连不通未测到；但按现有 `getDomainLabel` 会取二级标签 `google`（忽略子域名 flow），配合「无关即退域名」会得到 "Google" 而不是 "Flow"。
- **第四轮（用户要求把上面两个缺陷都修）**：
  - 应用商店链接：`isAppStoreUrl` + `extractAppNameFromUrl`，命中 `apps.apple.com/*/app/{slug}/idNNN` 时从 slug 取 App 名（`titleCaseSlug` 遇纯数字截断：`superset-100-coding-agents` → `Superset`），Google Play 从 `?id=` 包名取（剥掉 com/io/app 等通用段，`com.superset.app` → `Superset`）。这段放在 og:site_name 之前，避免 "App Store" 抢占。
  - 子域名：`getDomainLabel` → `getDomainLabels(url): string[]`，候选为 `[注册域, ...非通用子域名]`，`scoreAgainstDomain` 对所有候选取最高分；**兜底仍用注册域**（`news.ycombinator.com` 不会变成 "News"）。`GENERIC_SUBDOMAINS` 黑名单含 app/blog/news/docs/api/beta 等。
  - 20 个构造用例全过；真实站点 9 个中 App Store 已得 `Superset`。flow.google.com 沙箱连不通（离线兜底 Google），真机有 og:site_name/title 时会得 Flow。
- 用户随后提供了 flow.google.com 的真实 head（`<title>Google Flow - 打造视频…</title>`、`og:title=Google Flow - AI Creative Studio…`）：实测四种变体均得到 `Google Flow`（含 pure-中文在前也会跳到含品牌的那段）。` og:site_name` 存在时按其原值（本地化站点会是中文，符合既定优先级）。
- **第二批真实 URL 验证（11 个，PH 外链）**：10 个干净——simhadigital→Simha Digital(og)、plume.koumastudio.com→Plume(og)、oriane.xyz 子页→Oriane(title 匹配)、appgrowthkit→AppGrowthKit(og)、niubigeo.ai→NiubiGEO(title 末段匹配域名，无 og)、refoid.app→Refoid、faslaunch→FasLaunch、postraid→Postraid、ezgusavdo.uz→EzguSavdo、openvc.app 被 Cloudflare 403 但域名兜底→Openvc（官方写法 OpenVC，域名兜底无法还原大小写）。唯一瑕疵：omnidicom.com 的 `og:site_name = "OmniDICOM · 옴니다이콤"` 原样返回，多语言变体一起进了名字。**用户问「你怎么知道哪个是品牌名」并同意改动，遂实现 `pickPrimaryMetaName`**：与标题用同一套 `scoreAgainstDomain` 打分挑与域名最相关的段（韩/中译名在 `simplifyForCompare` 后为空 ⇒ 0 分天然被排除），全都 0 分则取第一段（品牌几乎总在前面，译名/标语在后），无分隔符原样返回。7 个新用例 + 21 个站点回归全过；`OmniDICOM · 옴니다이콤` → `OmniDICOM`，其余 20 个站点结果不变。
- **第三批真实 URL（24 个）暴露 3 个问题，均已修**：
  1. **真 bug：meta/title 在 `</head>` 之后**。zvonai.ai 有正常 `</head>`（6788 处）但 `<title>`/`og:*` 全渲染在 body（79190/81000 处，疑似模板把 head 标签写进 body）。原 `getHead` 只扫 head 导致 og/title 全丢，退化到域名 → `Zvonai`。改为 `scopes = [head, html]`，head 找不到再整篇扫；同时 `getHead` 在没有 `</head>` 时返回全文（原为 slice(0,20000)）。修后 `ZvonAI`。
  2. **代码托管链接**：`MARKETPLACE_HOST_SUFFIXES` 加上 github.com/gitlab.com，`/owner/repo` 取 repo 名（保留字路径如 /orgs/xxx 不算），`github.com/marsbos/zep` → `Zep`。原为 `GitHub`。
  3. **标题段含标语**：新增 `trimToDomainPrefix`，逐词累积恰好等于域名标签时截断，`KyvoLab Fintech Product Lab` → `KyvoLab`。原为整串。
  其余 21 个站点正确且回归无变化（Superset/Arcjet/…/Saladict）。仍偏"域名风"小写的是站点自己在 og/title 里就这么写（imado、wattai、scrape.land、milliseconds.ai、classonline.live）。
- **第四批真实 URL（用户贴回输出）19 个：17 个正确**，唯一错的是 `staiola.studio/apps/commentgrab` → `Staiola`（应 `CommentGrab`）。已修：新增「最后一个非通用路径分支」作为**最低优先级**候选（`getBrandedPathSegment` + `GENERIC_PATH_SEGMENTS`），接到 `getDomainLabels` 返回数组末尾；打分用严格 `>`，故注册域有匹配时不会被路径覆盖（oriane.xyz/free-tools/lead-sparker 仍为 `Oriane`），隐私政策/登录等通用路径被黑名单挡掉（maildoso/classonline 不变）。批内其余：`Creofeedback / Intentt`（Play 包名，网络不通靠 URL 兜底）、`PrimeSaaS / Bolus / Visitqo / PoloPrompt / Repodify / GetPaidly / Unpaged / SQLazy / ResumeCommand / VibeBridge / HRYP(check.hryp.com)`；偏长的 `Choppy Pilot Logbook`、`AI Album Cover Generator` 是站点自己 og:site_name 的写法；`payed.me` 的 og 就是 `payed.me`；`Teenstore / Pipelineapp(403) / Openvc(403)` 来自域名兜底。
- **第五批真实 URL（28 个）又暴露 3 处，均已修**：
  1. Apple slug `pome-ai-calorie-tracker` 被整段展开 → 新增 `SLUG_DESCRIPTOR_WORDS`（ai/app/tracker/generator…），slug 遇到数字或（非首词的）描述词就截断 → `Pome`；`superset-100-…` 仍 `Superset`。
  2. 托管平台域名（`meowmood-care.huangshihtsu.chatgpt.site`）：注册标签是平台名 `chatgpt`，品牌在最左标签 → 新增 `HOSTING_PLATFORM_SUFFIXES` + `getHostingPlatformBrandLabels`（必须 `.` 边界匹配，`superfly.dev` 不被 `fly.dev` 误判），直接返回左侧品牌标签 → `Meowmood Care`（此前是 `Cloudflare`）。
  3. `Cloudflare` 那个错的真正原因：拦截页标题 "Attention Required! | Cloudflare" 与 `meowmoodcare` 偶然共享 3 字符 `are`，弱信号阈值得分被选中 → 弱信号阈值从 3 提到 **4**。同时兜底标签支持连字符拆词大写（`meowmood-care` → `Meowmood Care`、`seadance-video` → `Seadance Video`）。
  - 批内其余正确：`BANKPILOT / Caybl.ai / Flawlee / GuruKool / Quovyn / Pluxee(in/products/…) / Posthaste / ArrivoBuddy / GTI Markets / LeakOps / TerriTool / Desk & Park / TripShare(sharetrip.site) / CallFirst / Qwarts Finance / Pealo / QuickMedCalc / Recovry / DNS Notify / WTS CRM / Vector0 / Forms Flow+ / Fibblix`；`Mtcleverrest`（Play 包名）、`Gethugpdf`（fetch 失败域名兜底）、`Caveat`（caveatpro 的 title 只写 Caveat）为可接受结果。16 个关键站点回归无变化。
- **第六批（51 个唯一 URL）修了 4 处**：
  1. `TITLE_SEPARATOR` 增加「冒号后带空格」（`Tulpa: Local-first…` 之前不切，整句被当成名字）和逗号（`AI Ebook Generator, Pay Per Book, No Subscription`）。`tulpatalk.com` → `Tulpa` ✓。
  2. Apple slug：描述词表不可能穷尽（`fractionsolver-math-step-calc` 一个都没命中）→ `titleCaseSlug` 改为「数字/非末位描述词截断 + 超过两个词只留前两词」。
  3. Gumroad 加入 marketplace：`*.gumroad.com/l/{slug}`、`gumroad.com/p/{slug}` → `Schemahand Offline`（原为 `Gumroad`）。
  4. 标题片段 >40 字符一律弃用退回域名（防 `create-ebook.com` 那种把整句标语当名字）。
  - 批内其余基本正确：`Atomira / UNSKIP / CBackup / HireLyf / nokko / ExemptMyHome / Anshkosh / Havnest Money / My Care Journals / OtoRank / Pocket PMO / Arteon / Seedance 2.5 / Sharebite / Bootstrapware / MemoFlow / Ghostkey / Coii AudioNotes / NexTranslate / Avionward / LEI Worldwide / Batesly / Scary Spider SEO / FixMyExport / Gaugely / Markubees / GrowVista / SociaFlow / Ilena / OzodFlow / PropertyOPA / 4free / Crecaly / Dropslim / Essacolor / Focuspath / Cosmomerge / Balancefeed / Trace / Keepfamilysafe Familysafety`。已知不完美：Apple 长 slug 仍会带一个描述词（`Shooal Dive`、`Bodybird Push`，因为保护 `Final Cut Pro`/`Candy Crush Saga` 这类多词品牌，只留两词）；`Apps by Mo`(appsbymo.app/margin)、`SagentLab`(sagentlab.com/navarch) 是 og:site_name 写工作室名，按「og 优先」保留；`image25.photo` 的 og 自身很长。
  - 16 个关键站点回归无变化；biome 曾因三元换行报格式错，已修。
- **第七批（38 个唯一 URL）修 2 处**：
  1. 托管平台分支之前提前 return，漏了路径产品名 → `ranintelligence.github.io/ikippa-site` 得 `Ranintelligence`；改为托管分支也追加 `getBrandedPathSegment`，现得 `iKippa` ✓。
  2. Vercel/Netlify 预览域名的部署哈希（`echo-echo-ae11.vercel.app` → `Echo Echo Ae11`）→ 新增 `DEPLOYMENT_HASH` + `stripDeploymentHash`，剥掉末尾「含数字的 4-10 位短串」；`daemon-os`、`cool-tool-123`、`meowmood-care` 不受影响。
  - 批内其余正确：`Gate Militia / ScheduleToCalendar / TopEdge / AIUpscaler.video / WeWin / BAGONSA / PDFAreUs / tagio.art / PDF Tools Base / ShopMate / Shufti / MediaScope / MediaPilot / TickerWhale / ARSOM LOYALTY / Socorin / Little Grimoire / DriveSight(phonedashcam 已改品牌名) / Tree Removal Cost Calculator / PortFoliQ / Routra / MyTripDude / Onlyon / Planning Data Hub / Calxo AI / TubePersona / GMapsScout / Mylegalguardian / Carop`。
  - 已知偏离：`fantasyroad.co.uk/comicz` → `Fantasy Road Comics`、`spaemanhawk.com/deskflow-landing-page` → `Spaeman Hawk`（og:site_name 写工作室名，按 og 优先保留）；`freedreaminterpreterai.com/zh` → `oneira`（og 本身是小写品牌名）。
- **全量回归（218 个已验证 URL 带期望值比对）**：第一次跑出 4 处不一致——其中 3 处是**变好**（keepfamilysafe → `KeepFamilySafe Family Safety`、essacolor → `EssaColor`、dropslim → `DropSlim`，之前那次抓取失败走的域名兜底，这次抓通拿到官方大小写）；1 处是**真回归**：`create-ebook.com` 因逗号切分后 `Pay Per Book`（与 `createebook` 共享 `book`，占 40%）抢在 `AI Ebook Generator`（占 33%）前面被选中 → 把弱信号占比阈值从 40% 提到 **50%**，两片段都不合格，退回 `Create Ebook` ✓。修正后重跑：**218 个中仅 1 个不一致，且是那次网络请求失败走离线兜底（essacolor），非逻辑问题**。
- **大幅简化（用户要求「别搞太复杂」）**：删掉全部基于域名的打分择优（`scoreAgainstDomain` / `longestCommonSubstringLength` / `trimToDomainPrefix` / `pickBrandFromTitle` / `pickPrimaryMetaName` / `GENERIC_PATH_SEGMENTS` / `getBrandedPathSegment` / `simplifyForCompare` / `TITLE_SEPARATOR`），改为极简管线：
  1. 平台宿主链接（App Store / Play / GitHub / GitLab / Gumroad）仍从 URL 取名（保留 `titleCaseSlug` + `SLUG_DESCRIPTOR_WORDS`）；
  2. `og:site_name` → `og:title` → `<title>`，每个值过 `cleanNameValue`：按 `| · • – — , : .（后接空格）` 切块，逐块用 `PAGE_NAME_PHRASES` + `PAGE_NAME_WORDS` 剥页面名词，**取第一个剥完还剩下的块**，全剥空则该来源作废；>60 字符也作废；
  3. 最后域名兜底（`getDomainLabels[0]` + `capitalizeLabel`）。
  - 顺带修：`403 / 404 / forbidden / cloudflare` 等错误页词进黑名单（否则 403 页输出 "403"）；`&` 这类纯符号 token 不再被剥掉（"Desk & Park"）。
  - 试过「含 `|` 时取最后一块」：修好 crecaly/gmapsscout/planningdatahub，但弄坏 glp1(→Perfected)/staiola/sf2systems/portfoliq，**已回退**，保持一律取第一块。
  - 中间还试过「整条保留、只剥页面名词」（用户原话：分隔符保留不要紧、前后不固定）→ 70 样本 **35 个变化**，名字普遍带上标语（"Radiobreak - The Commute Opener"），用户看完数据后**选择回退到「取第一块」**（方案 2，17 个变化）。
  - **代价（70 个关键样本回归，最终 17 个变化）**：变好 `glp1→Peptide Tracker`、`oriane/lead-sparker→Lead Sparker`、`gameplayer/nobrl→NovelMorrow`、`create-ebook→AI Ebook Generator`；变差 `niubigeo→中文描述片段`、`crecaly→Emoji copy paste keyword`、`gmapsscout`、`planningdatahub`、`check.hryp`、`pluxee`、`dejuncrane`、`saladict→AllenTown(翻回)`、`solluz→Solar Panel Company in India(翻回)`、`kyvolab→长名`。根本原因：去掉域名裁判后，「品牌在前」与「描述 | 品牌」无法自动判别。
- **送达率咨询（用户问「每封都有不同产品名，对避免进垃圾箱有没有用」）**：我的判断是——**有一点用但很小、且是间接的**。真正权重高的是：域名信誉 + SPF/DKIM/DMARC 对齐、预热与发送节奏、退信率/投诉率、互动率（回复）、链接与图片比例、tracking 域名。产品名的价值主要在于提升回复率 → 长期改善信誉；对相似度/聚类检测只有边际改善。反面风险：**名字不准比不加更糟**（"Hi KyvoLab Fintech Product Lab"、"Hi Apps by Mo"、"Hi 3 Ton Portable Gantry Crane" 一眼看出是抓的，容易被举报）。建议发信侧按名字质量分级使用，低质量名字退化为 "your team"。
- **收尾决策**：我提议「URL 路径段与标题首段精确相等时，允许标题压过 og:site_name」以修 `Margin/Navarch/DeskFlow` 这类工作室名案例，用户回复「还是保留大部分正确的逻辑吧」→ **不做这个改动**，保持 og:site_name 优先。最终策略定稿见 MEMORY.md。
- **第八批（37 个唯一 URL）修 2 处**：
  1. og:site_name 写成关键词短语的情况（`solluz.co.in` 的 og 是 `Solar Panel Company in India`）：新增规则——**≥4 个词且与域名零相关**时不采信，交给 title/域名 → `Solluz` ✓。
  2. 弱信号（公共子串）加了「占片段长度 40% 以上」的条件：`dejuncrane.com/news/3-ton-portable-gantry-crane-…` 的标题与 `dejuncrane` 只共享通用名词 `crane`（5/27 = 18%）→ 不再命中，得 `Dejuncrane` ✓；同时 `TripShare`(5/9=56%)、`Kinlet`(100%)、`Tree Removal Cost Calculator`(100%)、合成用例 `Acme CRM`(4/7=57%) 全部保持命中。
  - 顺手给 `GENERIC_PATH_SEGMENTS` 补了 start/welcome/onboarding/download/demo/checkout/cart/search/account/profile/settings/overview。
  - 批内其余正确：`Nino Cleaner / Color Personality Test / GlideNav / Claude Code Buster / NotchCraft / TaleScene / Fluorence.com / Amplifly / Agent Lint(github) / Image to Image / MarkdownViewer / RankRope / Conzent / Yieryi Meter / USFax / TrackMyDex / VM Hunter / Anime AI / Bookmark Cleaner / SF2 Systems / LC Shield / ShakeNotes / Thordata` 等；`Jagent(jev-agent.com 的 og)`、`Wakeel.org`、`TeaonherChecker`、`Reussitess®971`、`Image to Image` 是站点自报名字；`Okbuildpro / B1gplayer`(403)、`Axioncyber`、`Smdum / Adfindio`(Play 包名)、`xpaiming`(og 小写) 为可接受结果；`gameplayer.site/nobrl` → `Gameplayer`（og 无、标题是 NovelMorrow，属已知的「工作室域名」取舍）。
  - 又踩了一次 biome 格式（三元换行），已修。
- 注意：`src/lib/email-scraper.ts` 是 CRLF 文件，biome 默认 LF，所以 `biome check` 对该文件一直报 format 错误（既有问题，非本次引入）；用 LF 副本验证过新代码本身格式干净。另有既有 `noUnreachable` 报错（verifyEmail 里 return 之后的死代码）。
