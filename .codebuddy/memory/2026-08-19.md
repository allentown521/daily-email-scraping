# 2026-08-19

## 新增 productfame daily 选项
- 在 `src/components/common/main.tsx` 的 **Daily** 分组中新增 `productfame` 选项，打开 `https://productfame.com/trending`（静态 URL）。
- 改动点：selectedSites 状态、daily 分组选中逻辑、siteOptions、Daily 列 UI 列表。
