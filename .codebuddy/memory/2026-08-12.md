# 2026-08-12

## uneed content script 先展开全部产品再收集
- `src/app/uneed.content/index.tsx`：在收集 `ref=uneed.best` 链接前，先查找文本包含 "Show all products" 的 `<button>` 并点击展开全部产品，等待 3s 后再收集。找不到按钮则 warn 并按原样收集。
- 目的：避免只收集到折叠状态下可见的产品。
